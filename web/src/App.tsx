export function App() {
  return (
    <main>
      <h1>Cadence</h1>
      <p className="tagline">Your backlog, in flow.</p>
      <p className="note">
        Phase 0 scaffold — the gateway + web shell are wired. The themed app shell and
        live data wiring arrive in steps 0.6–0.7.
      </p>
    </main>
  );
}
