# Dependencies
node_modules/

# Build output
dist/
build/
out/

# Tauri build artifacts (generated; never committed). NOTE: src-tauri/Cargo.lock IS tracked
# (it's an application binary) and is intentionally NOT ignored here.
src-tauri/target/
src-tauri/binaries/
src-tauri/resources/
src-tauri/gen/

# Local app data / SQLite (Cadence stores data under ~/.cadence, but ignore any local copies)
*.db
*.sqlite
*.sqlite3

# Env & secrets — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
*.p12
id_rsa*
*.secret
secrets/
*.local.json

# App/runtime data must never enter the repo (it lives in ~/.cadence)
/.cadence/
.cadence/

# Logs
*.log
logs/

# Local tooling / runtime state (Claude Code session locks, etc.)
.claude/

# OS / editor cruft
.DS_Store
.vscode/
.idea/
*.swp
